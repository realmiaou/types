workflow:
  rules:
    - if: $CI_COMMIT_BRANCH

variables:
  NPM_TOKEN: $NPM_TOKEN

stages:
  - release

publish:
  image: node:latest
  stage: release
  script:
    - npm install
    - npm run semantic-release
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
