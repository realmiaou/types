workflow:
  rules:
    - if: $CI_COMMIT_BRANCH

variables:
  NPM_TOKEN: $NPM_TOKEN

stages:
  - release

publish:
  image: node:16-alpine
  stage: release
  script:
    - yarn install
    - yarn semantic-release
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
